// Declare variables, asking for user input

const username = prompt("What is your name?");
const age = prompt("What is your age?");
const hobby = prompt("What is your hobby?");

// Create sentence and save to a new variable

let info = "You are " + username + ". You are " + age + " years old." + "You love " + hobby + ".";

// Console log

console.log(info);

// Alert 

alert(info);